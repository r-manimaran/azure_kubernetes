apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-file-logging-pvc
  namespace: gracefulshutdown
spec:
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""
  volumeName: azure-file-pv
  accessModes:
    - ReadWriteMany
